package com.dome.sdkserver.metadata.dao.mapper;

import com.dome.sdkserver.bo.ChannelManager;

import java.util.List;

public interface ChannelManagerMapper {

    List<ChannelManager> queryList(ChannelManager channelManager);

    int deleteById(Integer id);

    int addChannel(ChannelManager channelManager);
}

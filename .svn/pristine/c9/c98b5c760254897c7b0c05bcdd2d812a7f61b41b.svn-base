package com.dome.sdkserver.metadata.dao.mapper.sign;

import com.dome.sdkserver.metadata.entity.sign.SignEntity;
import org.springframework.stereotype.Repository;

/**
 * Created by xuekuan on 2017/3/29.
 */
@Repository("signMapper")
public interface SignMapper {
    /**
     * 新增完成任务用户
     */
    public int insert(SignEntity signEntity);

    Integer queryByUserId(SignEntity signEntity);
}
